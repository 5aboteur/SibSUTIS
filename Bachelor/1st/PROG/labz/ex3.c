#include <stdio.h>
int main()
{

float fact1, t, fact2, fx, xi, eps, x;
int i, k;
printf("Input eps, x: ");
scanf("%f %f", &eps &x);

t=1;
fact1=2*i;
fact2=(1-2*i);
fx=0;
xi=x;
k=1;
i=0;

while (eps>fx)

	if (k==1)
	{
	fx=1+( );
	k=-1;
	}
	else
	{
	fx=1-
	k=1;
	}

	xn=xn*x;
	t=t*4*i;
	i++; 
	
